# Optional. Without this, Humaniser still runs the offline rules engine.
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
PORT=8787
HUMANISER_MODEL=claude-opus-5
